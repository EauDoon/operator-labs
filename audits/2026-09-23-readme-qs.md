# README Quick Start Audit - 2026-09-23

Repo: operator-labs
Default branch: main

Finding: Quick start already present within first 60 lines.

Existing surface:
- Lines 16 to 21: install block for both subpackages
  (python -m pip install ./packages/corridor-lab and ./packages/tracecanary).
- Lines 26 to 75: worked cases with run commands for corridor-lab and tracecanary.

Concrete steps visible in first 60 lines:
1. python -m pip install ./packages/corridor-lab
2. python -m pip install ./packages/tracecanary
3. PYTHONPATH=src; python -m corridor_lab compare ...
4. PYTHONPATH=src; python -m tracecanary diff ...

Action: surface-confirmed. Skip.